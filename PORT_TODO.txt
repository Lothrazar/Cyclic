
Large unknowns:
- Item NBT data is totally broken. ? a DataComponent
- All recipe registry is broken
- All network/server packets are broken. import net.minecraftforge.network.NetworkEvent is gone
- RecipeSerializer is gone, need full rewrite with new fancy Codec junk
- Cable blocked (yellow) state needs full rewrite
- Configs? "Cannot resolve method 'setConfig' in 'ConfigSpec'"
- Enchantments need full rewrite, maybe standalone
- CyclicMobEffect ticking-potions full rewrite
-


Small unknowns:
- ItemStack.isEdible()  is just plain removed? what is food even? RESOLVED:  probably getFoodProperties==null
- ForgeHooks.getBurnTime   gone is there a NeoForgeHooks ? RESOLVED: use stack version
- MenuScreens.register broken
- machine blocks used to nuke capability when  POWERCONF.get() > 0; need a new system for this
- NetworkHooks.openScreen is gone, how do items/blocks open their screens now?
- recipes using Fluid Tags needs testing
- investigate BlockEntity.INFINITE_EXTENT_AABB
- where is ForgeEventFactory
- Why dont energy items need this anymore. is there any replacement after full delete
@Override
public ICapabilityProvider initCapabilities(ItemStack stack, CompoundTag nbt) {
 return new FluidHandlerCapabilityStack(stack, TileCask.CAPACITY);
}
- look into RegisterCapabilitiesEvent : registerItem
- block.randomTick fix
- ToolActions.DEFAULT_SHIELD_ACTIONS is gone
- Shield IClientItemExtensions is gone


References
https://github.com/neoforged/.github/blob/main/primers/1.21/index.md
https://docs.neoforged.net/docs/gettingstarted

